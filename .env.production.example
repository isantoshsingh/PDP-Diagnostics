# Prowl — DigitalOcean Production Environment Variables
# Copy this file to .env and fill in actual values before deploying.
# These values are read by Kamal via .kamal/secrets at deploy time.

# ── DigitalOcean Droplet ──────────────────────────────────────────
# IP address of your DigitalOcean droplet
DEPLOY_HOST=your.droplet.ip.address

# ── Container Registry (GitHub Container Registry) ────────────────
# GitHub username (or org) for pushing Docker images
KAMAL_REGISTRY_USERNAME=your-github-username
# GitHub personal access token with write:packages scope
KAMAL_REGISTRY_PASSWORD=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Rails ─────────────────────────────────────────────────────────
# From config/master.key (never commit this file)
RAILS_MASTER_KEY=your-rails-master-key

# ── Database ──────────────────────────────────────────────────────
# PostgreSQL connection string (Kamal accessory runs on the same droplet)
DATABASE_URL=postgres://prowl:your-db-password@prowl-db:5432/prowl_production
# Password for the PostgreSQL container managed by Kamal
POSTGRES_PASSWORD=your-db-password

# ── Shopify App Credentials ───────────────────────────────────────
# From shopify.app.prod.toml / Shopify Partners Dashboard
SHOPIFY_API_KEY=45aaac72a33c1acaa7128a33239d3d40
SHOPIFY_API_SECRET=your-shopify-api-secret

# ── Application ───────────────────────────────────────────────────
# Public URL where the app is accessible (must match Shopify app config)
HOST=https://prowl.lucyapps.com
